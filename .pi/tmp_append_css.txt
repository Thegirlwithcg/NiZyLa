
/* Obsidian-like frameless shell */
.app-titlebar { padding-right: 138px; }
.window-control-box {
  -webkit-app-region: no-drag;
  position: fixed;
  top: 0;
  right: 0;
  height: 40px;
  display: grid;
  grid-template-columns: repeat(3, 46px);
  z-index: 1000;
  background: #202020;
}
.window-control-box button {
  border: 0;
  border-radius: 0;
  padding: 0;
  background: transparent;
  color: var(--muted);
  font: 15px/1 system-ui, sans-serif;
}
.window-control-box button:hover { background: #363636; color: var(--text); }
.window-control-box button.close:hover { background: #c42b1c; color: #fff; }
.activity-bar {
  -webkit-app-region: no-drag;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 6px;
  padding: 8px 6px;
  border-right: 1px solid var(--border);
  background: #202020;
}
.activity-bar button {
  width: 34px;
  height: 34px;
  display: grid;
  place-items: center;
  border: 0;
  border-radius: 5px;
  padding: 0;
  background: transparent;
  color: var(--muted);
  font-size: 16px;
}
.activity-bar button:hover, .activity-bar button.active { background: #303030; color: var(--text); }
.document-search { display: grid; grid-template-rows: auto auto 1fr; min-height: 0; height: 100%; padding: 10px; gap: 10px; }
.search-row { display: grid; grid-template-columns: minmax(0, 1fr) 108px; gap: 8px; }
.search-row input, .search-row select {
  min-width: 0;
  border: 1px solid var(--border);
  border-radius: 5px;
  padding: 7px 9px;
  background: #292929;
  color: var(--text);
}
.search-meta { color: var(--muted); font-size: 12px; }
.search-results { overflow: auto; display: grid; align-content: start; gap: 4px; }
.search-result {
  display: grid;
  gap: 3px;
  width: 100%;
  border: 0;
  border-radius: 5px;
  padding: 8px;
  background: transparent;
  color: var(--text);
  text-align: left;
}
.search-result:hover { background: #303030; }
.search-result strong { font-size: 13px; font-weight: 600; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.search-result span { color: var(--muted); font-size: 11px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.search-result em { color: #b8b8b8; font-size: 12px; font-style: normal; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.theme-obsidian .workspace { background: #1e1e1e; }
.theme-obsidian .brand { min-width: 145px; }
.theme-obsidian .brand .logo { width: 24px; height: 24px; }
.theme-obsidian .brand span { display: none; }
.theme-obsidian .actions button, .theme-obsidian .actions select { background: transparent; border-color: transparent; color: var(--muted); }
.theme-obsidian .actions button:hover, .theme-obsidian .actions select:hover { background: #303030; color: var(--text); border-color: #303030; }
.theme-obsidian .actions .primary, .theme-obsidian .actions .active { background: #303030; color: var(--text); }
